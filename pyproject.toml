[project]
name = "mireye-mcp"
version = "0.5.0"
description = "MCP server for Mireye Earth — federal-source-cited geospatial data for any MCP-aware agent."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Mireye" }]
keywords = ["mcp", "model-context-protocol", "geospatial", "claude", "cursor", "mireye"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Operating System :: OS Independent",
]
dependencies = [
    "httpx>=0.27",
    "mcp>=1.27.2,<2.0",
]

[project.urls]
Homepage = "https://api.mireye.com"
Repository = "https://github.com/Mireye-Labs/mireye-earth-mcp"

[project.scripts]
mireye-mcp = "mireye_earth_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mireye_earth_mcp"]
